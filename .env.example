# Обязателен в продакшене: длинная случайная строка (например, `openssl rand -hex 32`)
JWT_SECRET=замени-на-длинную-случайную-строку

# Смещение игрового дня (Китай = UTC+8)
TZ_OFFSET_HOURS=8

PORT=3000
